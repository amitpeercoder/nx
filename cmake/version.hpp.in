#pragma once

// Auto-generated version header - DO NOT EDIT
// Generated by GitVersion.cmake from version.hpp.in

#include <string>
#include <string_view>

namespace nx {

struct GitVersionInfo {
    static constexpr int major = @GIT_VERSION_MAJOR@;
    static constexpr int minor = @GIT_VERSION_MINOR@;
    static constexpr int patch = @GIT_VERSION_PATCH@;
    static constexpr std::string_view prerelease = "@GIT_VERSION_PRERELEASE@";
    static constexpr std::string_view build = "@GIT_VERSION_BUILD@";
    static constexpr std::string_view full = "@GIT_VERSION_FULL@";
    static constexpr std::string_view commit_hash = "@GIT_COMMIT_HASH@";
    static constexpr bool is_dirty = @GIT_IS_DIRTY_BOOL@;
    static constexpr std::string_view version_type = "@VERSION_TYPE@";
    
    // Commits since last tag (0 if on tag, empty string if no tags)
    static constexpr std::string_view commits_since_tag = "@GIT_COMMITS_SINCE_TAG@";
    
    // Convenience methods
    static std::string getVersionString() {
        return std::string(full);
    }
    
    static std::string getShortVersion() {
        return std::to_string(major) + "." + std::to_string(minor) + "." + std::to_string(patch);
    }
    
    static bool isRelease() {
        return version_type == "release";
    }
    
    static bool isDevelopment() {
        return version_type == "development" || version_type == "debug";
    }
    
    static bool isCIBuild() {
        return version_type == "ci";
    }
    
    static bool isDirty() {
        return is_dirty;
    }
};

} // namespace nx